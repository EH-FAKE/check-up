[pytest]
python_files = test_*.py
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests (require real environment)
filterwarnings =
    ignore::DeprecationWarning:models
    ignore::DeprecationWarning:sqlalchemy
